---
import "../styles/global.css";

import BaseLayout from '../layouts/BaseLayout.astro';
import es from '../i18n/es';
import Hero from '../components/Hero.astro';

const lang = 'es';
const isDark = true; // luego esto se puede cambiar con un botón o preferencia
const t = es;
---

<BaseLayout lang={lang} isDark={isDark}>
  <Hero t={t} />
</BaseLayout>
